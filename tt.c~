#include <stdio.h>
void test(int i)
{
  int a=0x123;
  if(i>0)
    {
      int b=0x2345;
      printf("%p\n",&b);
    }
  int c=0x789;
  printf("%p %p\n",&a,&c);
}
void main()
{
  test(1);
  test(-1);
}
